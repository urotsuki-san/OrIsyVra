# Windows release note

Windows binaries now include PE product metadata and the installer defaults to per-user installation. The build workflow can Authenticode-sign the executables and installer when trusted signing credentials are configured.
